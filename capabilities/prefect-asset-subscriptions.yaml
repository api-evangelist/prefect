naftiko: 1.0.0-alpha2
info:
  label: Prefect Cloud API — Asset Subscriptions
  description: 'Prefect Cloud API — Asset Subscriptions. 1 operations. Lead operation: Create Asset Subscription. Self-contained
    Naftiko capability covering one Prefect business surface.'
  tags:
  - Prefect
  - Asset Subscriptions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PREFECT_API_KEY: PREFECT_API_KEY
capability:
  consumes:
  - type: http
    namespace: prefect-asset-subscriptions
    baseUri: ''
    description: Prefect Cloud API — Asset Subscriptions business capability. Self-contained, no shared references.
    resources:
    - name: api-accounts-account_id-workspaces-workspace_id-asset-subscriptions
      path: /api/accounts/{account_id}/workspaces/{workspace_id}/asset-subscriptions/
      operations:
      - name: createassetsubscriptionapiaccountsaccountidworkspacesworkspaceidassetsubscriptionspost
        method: POST
        description: Create Asset Subscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account_id
          in: path
          type: string
          required: true
        - name: workspace_id
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: prefect-asset-subscriptions-rest
    port: 8080
    description: REST adapter for Prefect Cloud API — Asset Subscriptions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/accounts/{account-id}/workspaces/{workspace-id}/asset-subscriptions
      name: api-accounts-account-id-workspaces-workspace-id-asset-subscriptions
      description: REST surface for api-accounts-account_id-workspaces-workspace_id-asset-subscriptions.
      operations:
      - method: POST
        name: createassetsubscriptionapiaccountsaccountidworkspacesworkspaceidassetsubscriptionspost
        description: Create Asset Subscription
        call: prefect-asset-subscriptions.createassetsubscriptionapiaccountsaccountidworkspacesworkspaceidassetsubscriptionspost
        with:
          account_id: rest.account_id
          workspace_id: rest.workspace_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: prefect-asset-subscriptions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Prefect Cloud API — Asset Subscriptions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: create-asset-subscription
      description: Create Asset Subscription
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: prefect-asset-subscriptions.createassetsubscriptionapiaccountsaccountidworkspacesworkspaceidassetsubscriptionspost
      with:
        account_id: tools.account_id
        workspace_id: tools.workspace_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
