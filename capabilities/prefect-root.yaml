naftiko: 1.0.0-alpha2
info:
  label: Prefect Cloud API — Root
  description: 'Prefect Cloud API — Root. 1 operations. Lead operation: Hello. Self-contained Naftiko capability covering
    one Prefect business surface.'
  tags:
  - Prefect
  - Root
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PREFECT_API_KEY: PREFECT_API_KEY
capability:
  consumes:
  - type: http
    namespace: prefect-root
    baseUri: ''
    description: Prefect Cloud API — Root business capability. Self-contained, no shared references.
    resources:
    - name: api-accounts-account_id-workspaces-workspace_id-hello
      path: /api/accounts/{account_id}/workspaces/{workspace_id}/hello
      operations:
      - name: helloapiaccountsaccountidworkspacesworkspaceidhelloget
        method: GET
        description: Hello
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account_id
          in: path
          type: string
          required: true
        - name: workspace_id
          in: path
          type: string
          required: true
        - name: x-prefect-api-version
          in: header
          type: string
  exposes:
  - type: rest
    namespace: prefect-root-rest
    port: 8080
    description: REST adapter for Prefect Cloud API — Root. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/accounts/{account-id}/workspaces/{workspace-id}/hello
      name: api-accounts-account-id-workspaces-workspace-id-hello
      description: REST surface for api-accounts-account_id-workspaces-workspace_id-hello.
      operations:
      - method: GET
        name: helloapiaccountsaccountidworkspacesworkspaceidhelloget
        description: Hello
        call: prefect-root.helloapiaccountsaccountidworkspacesworkspaceidhelloget
        with:
          account_id: rest.account_id
          workspace_id: rest.workspace_id
          x-prefect-api-version: rest.x-prefect-api-version
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: prefect-root-mcp
    port: 9090
    transport: http
    description: MCP adapter for Prefect Cloud API — Root. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: hello
      description: Hello
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prefect-root.helloapiaccountsaccountidworkspacesworkspaceidhelloget
      with:
        account_id: tools.account_id
        workspace_id: tools.workspace_id
        x-prefect-api-version: tools.x-prefect-api-version
      outputParameters:
      - type: object
        mapping: $.
