naftiko: 1.0.0-alpha2
info:
  label: Prefect Cloud API — Workspace Scopes
  description: 'Prefect Cloud API — Workspace Scopes. 1 operations. Lead operation: Get Workspace Scopes. Self-contained Naftiko
    capability covering one Prefect business surface.'
  tags:
  - Prefect
  - Workspace Scopes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PREFECT_API_KEY: PREFECT_API_KEY
capability:
  consumes:
  - type: http
    namespace: prefect-workspace-scopes
    baseUri: ''
    description: Prefect Cloud API — Workspace Scopes business capability. Self-contained, no shared references.
    resources:
    - name: api-workspace_scopes
      path: /api/workspace_scopes
      operations:
      - name: getworkspacescopesapiworkspacescopesget
        method: GET
        description: Get Workspace Scopes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: prefect-workspace-scopes-rest
    port: 8080
    description: REST adapter for Prefect Cloud API — Workspace Scopes. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/workspace-scopes
      name: api-workspace-scopes
      description: REST surface for api-workspace_scopes.
      operations:
      - method: GET
        name: getworkspacescopesapiworkspacescopesget
        description: Get Workspace Scopes
        call: prefect-workspace-scopes.getworkspacescopesapiworkspacescopesget
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: prefect-workspace-scopes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Prefect Cloud API — Workspace Scopes. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-workspace-scopes
      description: Get Workspace Scopes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prefect-workspace-scopes.getworkspacescopesapiworkspacescopesget
      outputParameters:
      - type: object
        mapping: $.
